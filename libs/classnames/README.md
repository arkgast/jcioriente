# classnames

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build classnames` to build the library.

## Running unit tests

Run `nx test classnames` to execute the unit tests via [Jest](https://jestjs.io).
