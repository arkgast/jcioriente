# JCI Oriente

## Commands

    $ cd jcioriente

    $ yarn

    $ nx serve spotlight

### Add new projects


    $ nx g @nx/react:app <app name> --bundler vite

    $ nx g @nx/react:lib <lib name> --bundler vite

    $ nx g @nx/react:setup-tailwind --project=<app name>
